// This script allows for mouse movement effects on the moon if desired.
// If you prefer the moon to be stationary, you can comment out the code below.

// Make the moon slowly move (optional)
// document.addEventListener("mousemove", function(event) {
//     let moon = document.querySelector(".moon");
//     let x = (event.clientX / window.innerWidth) * 100;
//     let y = (event.clientY / window.innerHeight) * 100;
//     moon.style.transform = `translate(${x-50}%, ${y-50}%)`;
// });
